import 'dart:io';

import 'package:sqflite/sqflite.dart';

class LocalDataApp {
  // static Database? _db;
  Database? _db;

  close() async {
    // var db = await this.database();
    //   var result = db!.close();
    //  return result;
  }
}
